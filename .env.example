# Local development only. Do not reuse these credentials in production.
POSTGRES_DB=lifeos
POSTGRES_USER=lifeos
POSTGRES_PASSWORD=lifeos_dev_only
POSTGRES_PORT=5432
DATABASE_URL=postgresql://lifeos:lifeos_dev_only@localhost:5432/lifeos

# API
HOST=0.0.0.0
PORT=4000

# Web: leave blank to use same-origin contract / Vite dev proxy.
VITE_API_URL=
